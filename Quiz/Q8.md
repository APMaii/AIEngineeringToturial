#  Q8


Dar in quiz talash mikonim betonoim protocole ML ro yad gerefte bashim 



Say konid Yeki az 4 dataye ghabli k dar Q6 sohbat shode ( ya harchi bishtar behtar) ro bardarid va improt konid , cleaning anjam bedid va aval 2 model entekhab konid ( hata bishtar ham entekhab kardid hata behtar). 

Pishnehade man SVR va DecisionTree hast , ama SGD , Ridge() va .. ham gozine haye awli ee hastand. 


Sepas be se raveshe motefavet train anjam bedid .



## Raveshe aval : Train Test split

ebteda daghighan mananade darsname ML_Models.ipynb va AI_Ml_intro.ipynb , kafi hast train test split konid

```python

#Import data------------

#Cleaning the data------------

#Split to numpy x and numpy y ------------


#train test split ------------
from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test = train_test_split(......)

# Selection model and hyperparameter ------------
from sklearn.something import one_model

model = one_model(hyperpparametr1, hyperparameter2,.....)


#fitting on training
model.fit(x_train,y_train)



#Evaluation on test and train ------------
y_train_pred  = model.predict(x_train)
y_test_pred = model.predict(x_test)

from sklearn.metrics import one_metric_formula

felan_train_score = one_metric_formula(y_train, y_train_pred)

felan_test_score = one_metric_formula(y_test, y_test_pred)


#print and plot ------------------------


```


## Raveshe Dovom : GridSearch CV

dar in ravesh bar asase AI_ML_GridsearchCV k be onvane GHoole dovom azash yad mishe estefade konid va baraye hamoon dota model , yek range e khobi az hypeparametr ha entekhab konid baraye gridsearch (injasho fght mitonid az gpt komak begirid )


```python

#Import data------------

#Cleaning the data------------

#Split to numpy x and numpy y ------------


#train test split ------------
from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test = train_test_split(......)



#---Gridsearch and model selection

from sklearn.something import one_model

model = one_model(default hyperparametr if needed...)

hyperparameter_configurations={
	'hyperparametr1': [value1,value2,....],
	'hyperparametr2': [value1,....],
	'....' : [.....],

}


from sklearn.model_selection import GridSearchCV

grid = GridSearchCV(
	estimator = model, 
	param_grid =hyperparameter_configurations,
	cv = one_value,
	scoring = one_value )


#---Training and fine tuning

grid.fit(x_train,y_train)


#----results

grid.best_score_

grid.best_params_





#------test score
best_model = grid.best_estimator_

pred = best_model.predict(x_test)


from sklearn.metrics import one_metric_formula


test_score_felan = one_metric_formula(y_test,pred)


#-----print and plot------


```





## Raveshe Sevom : Pipeline

in dg ghoole marhale akhar hast va mohem tarin ghesmatemon hast va shoam bayad az hamon dota model bar asase result ha oni k behtarin hast yedone model ro fght entekhab konido pipeline ro roosh ejra konid , 

bar asase darsname ye AI_ML_Pipeline.ipynb jolo berid va hamchin chizi hast ,bar asase datatoon k ch soton hae dare mitoonid bar asase AI_ML_Feature_Engineering jolo berid baraye entekhabe preprocessore monaseb (ya hata az gpt komak begirid ama na besorate codi , besorate inke begid baraye chenin datahaee ch chiz hae niaze)



```python


#Import data------------

#Cleaning the data------------

#Split to numpy x and numpy y ------------



#train test split ------------
from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test = train_test_split(......)





#Sakhte Pipeline (model,procesor)
from sklearn.something import preprocessor1
from sklearn.something import preprocessor2

from sklearn.something import model 


preprocessor1 = preprocessor1(......)
preprocessor2 = preprocessor2(......)


model = model(.......)


from sklearn.pipeline import Pipeline

pipe = Pipeline( [  ('preprocessor1',preprocessor1) ,
					 ('preprocessor2',preprocessor2),
					 ('model',model)     ])





#---Gridsearch  on Pipe


hyperparameter_configurations={
	'preprocessor1__hyperparametr1': [value1,value2,....],
	'preprocessor2__hyperparametr2': [value1,....],
	'model__hyperparametr1' : [value1m,....]
	'....' : [.....],

}


from sklearn.model_selection import GridSearchCV

grid = GridSearchCV(
	estimator = pipe, 
	param_grid =hyperparameter_configurations,
	cv = one_value,
	scoring = one_value )



grid.fit(x_train,y_train)

grid.best_params_
grid.best_score_



#-----evaluation-------

from sklearn.metrics import one_metric_formula


y_pred = grid.predict(x_test)

something_test_score = one_metric_formula(t_train,y_ped)


#-----print and plot------



```











